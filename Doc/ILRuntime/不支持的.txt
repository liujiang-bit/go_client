1. Enum.IsDefine 目前不支持
2. Hotfix不要去写继承主工程的东西，如果需要，请联系我。
3. 对于值类型的变量，如果是放在某个类里面，请取出来，再传递给主工程的函数使用
4. 有需要使用主工程新的模板函数或者模板类的，也是联系我添加支持
5. 不要使用struct， 实在需要用的时候，需要通知我本人，由我来确认需求。
6. 枚举类型不要使用 CompareTo
7 枚举只支持这几个函数 Parse  GetValues GetNames GetName ToObject