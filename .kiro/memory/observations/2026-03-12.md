# 观察记录 - 2026-03-12

---

### [OBS-0001] 🔵 claude-mem 架构分析

- **时间**: 2026-03-12
- **类型**: discovery
- **文件**: .kiro/claude-mem-main/
- **概念**: claude-mem, memory-system, architecture, hooks, sqlite, mcp

**摘要**: 完整分析了 claude-mem 持久记忆系统的架构，包括 5 个生命周期 Hook、Worker Service（Express on port 37777）、SQLite + Chroma 双存储、Claude Agent SDK AI 压缩、MCP 搜索工具、CLAUDE.md 上下文注入机制。

**详情**:
claude-mem 核心数据流：PostToolUse hook 捕获工具使用 → Worker Service 接收 → Claude Agent SDK 压缩为结构化 observation（type/title/subtitle/narrative/facts/concepts/files） → SQLite 存储 → SessionStart hook 查询并注入 CLAUDE.md。搜索通过 MCP Server 提供 3 层 workflow（search → timeline → get_observations），实现 10x token 节省。依赖链：Node.js + Bun + Python(uv) + SQLite + Chroma，基础设施较重。

---

### [OBS-0002] 🟣 Kiro 原生记忆系统实现

- **时间**: 2026-03-12
- **类型**: feature
- **文件**: .kiro/steering/kiro-memory-system.md, .kiro/skills/kiro-memory/SKILL.md, .kiro/hooks/memory-capture.kiro.hook, .kiro/memory/
- **概念**: kiro-memory, steering, hooks, skills, memory-system

**摘要**: 将 claude-mem 的核心理念适配为 Kiro 原生实现，零外部依赖，使用 steering（自动注入）+ skill（操作手册）+ hook（agentStop 触发）+ markdown 文件存储。

**详情**:
适配映射：SQLite → markdown 文件按日期分组；Worker + AI 压缩 → agentStop hook 让 Kiro 自评估；CLAUDE.md 注入 → auto inclusion steering；MCP 搜索 → timeline.md + index.md 索引。存储结构：observations/（按日期）、summaries/latest.md、timeline.md（最近30条）、index.md（按类型分类）。观察类型保留 claude-mem 的 6 种：discovery/bugfix/feature/decision/change/refactor。

---
